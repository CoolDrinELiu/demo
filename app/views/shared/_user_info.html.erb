<%= link_to gravatar_for(current_user, size: 50), current_user, style: "float: left;" %>
<h1 style="display: inline;"><%= current_user.name %></h1>
<span style="display: block;"><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
